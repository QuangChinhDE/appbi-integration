FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json ./
COPY apps/web/package.json apps/web/package-lock.json ./apps/web/

RUN npm ci

EXPOSE 3002

CMD ["npm", "--workspace", "apps/web", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3002"]